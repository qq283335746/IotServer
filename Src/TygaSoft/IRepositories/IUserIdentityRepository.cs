using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using TygaSoft.Model.DbTables;

namespace TygaSoft.IRepositories
{
    public interface IUserIdentityRepository
    {
        // UserIdentityInfo GetUserIdentityInfo(int applicationId, string userId);
        // Task<int> DeleteAsync(int applicationId, string userName);
        // Task<int> InsertAsync(UserIdentityInfo model);
        // Task<int> UpdateAsync(UserIdentityInfo model);
    }
}